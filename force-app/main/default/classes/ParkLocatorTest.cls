/**
 * @author Michael Stallings
 * @date 01/03/2021
 * @group HTTP Requests SOAP TEST
 * @see JIRA-123
 * @description Test for Apex Class making a callout to a SOAP endpoint utilizing WSDL Classes generated by WSDL2Apex
 */

@isTest
private class ParkLocatorTest {
    
    @isTest static void testCallout () {
        Test.setMock(WebServiceMock.class, new ParkServiceMock());

        String country = 'United States';
        List<String> result = ParkLocator.country(country);
        List<String> parks = new List<String>();
        parks.add('Yosemite');
        parks.add('Yellowstone');
        parks.add('Another Park');

        System.assertEquals(parks, result);
    }
}
